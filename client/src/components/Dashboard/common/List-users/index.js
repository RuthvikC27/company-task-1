import React from 'react';
import Nav from '../Nav';

const Listusers = ({ role }) => {
    return (
        <React.Fragment>
            <Nav role={role}/>
        </React.Fragment>
    );
};


export default Listusers;