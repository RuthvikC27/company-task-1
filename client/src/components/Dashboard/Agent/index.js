import React from 'react';
import Nav from '../common/Nav';

const Agent = ({ role }) => {


    return (
        <React.Fragment>
            <Nav role={role}/>
            Agent
        </React.Fragment>
    );
};

export default Agent;